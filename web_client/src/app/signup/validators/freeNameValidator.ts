import { FormGroup } from '@angular/forms';


export function freeNameValidator() {
    return (formGroup: FormGroup) => {

        // if (varOne.value) {
        //     varOne.setErrors({ free: true });
        // } else {
        //     varOne.setErrors(null);
        // }
    };
}
